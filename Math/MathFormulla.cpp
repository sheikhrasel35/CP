1 + r + r2 + ... + r^n = SUM(k=0...n) r^k = (1 - r^n+1) /(1 - r), and
r + r^2 + r^3 + ... + r^n = SUM(k=1...n) r^k = (r - r^n+1)/(1 - r).
Sum of XOR of all possible subsets=(Or of all elements)*(2^(n-1));
sum of sum of all sub-sequences=(sum of all elements)*(2^(n-1));
XOR of the XOR’s of all subsets=if(n==1) s[0] ,else 0;
Artithmatic Sequence :
 n'th term - a+(n-1)*d.
 sum of n numbers - n*(2*a+(n-1)*d)/2.
*For all i=0 to k,
Summation of (n+i) C (n) =(n+k+1) C (n+1)*